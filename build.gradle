plugins {
    id "com.bmuschko.vagrant-base" version "2.1"
}
vagrant {
    installation {
        validate = false
    }
}
import com.bmuschko.gradle.vagrant.tasks.*

task vstatus(type: VagrantStatus) {

}